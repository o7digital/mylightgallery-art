<section class="hero">
  <div class="hero-content">
    <h1>Galeria de Arte en el Corazon de Irving, Texas</h1>
    <p class="subtitle">ESTABLECIDA 2026</p>
    <p>Una galería familiar especializada en obras de arte contemporáneo y clásico de la más alta calidad.</p>
  </div>
  <div class="hero-newsletter">
    <a href="/newsletter" class="newsletter-link">Suscribe nuestra newsletter</a>
  </div>
  <div class="hero-scroll">
    <a href="#colecciones" class="scroll-link">
      <span class="scroll-text">See More</span>
      <svg class="scroll-arrow" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M12 5v14M19 12l-7 7-7-7"/>
      </svg>
    </a>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: #f5f5f5;
    padding: 4rem 2rem;
    position: relative;
  }

  .hero-content {
    max-width: 800px;
    text-align: center;
  }

  h1 {
    font-size: clamp(2rem, 5vw, 3.5rem);
    font-weight: 300;
    letter-spacing: 0.05em;
    margin-bottom: 1.5rem;
    line-height: 1.2;
  }

  .subtitle {
    font-size: 1.2rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    margin-bottom: 2rem;
  }

  p {
    font-size: 1.1rem;
    line-height: 1.6;
    margin-bottom: 2rem;
    color: #555;
  }

  .btn-primary {
    display: inline-block;
    padding: 1rem 2rem;
    background: #000;
    color: white;
    text-decoration: none;
    transition: background 0.3s;
  }

  .btn-primary:hover {
    background: #333;
  }

  .hero-newsletter {
    position: absolute;
    bottom: 2rem;
    left: 2rem;
  }

  .newsletter-link {
    color: #333;
    text-decoration: none;
    font-size: 0.95rem;
    transition: color 0.3s;
    border-bottom: 1px solid #333;
    padding-bottom: 2px;
  }

  .newsletter-link:hover {
    color: #000;
    border-bottom-color: #000;
  }

  .hero-scroll {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
  }

  .scroll-link {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    color: #333;
    text-decoration: none;
    transition: color 0.3s;
  }

  .scroll-link:hover {
    color: #000;
  }

  .scroll-text {
    font-size: 0.9rem;
    letter-spacing: 0.05em;
  }

  .scroll-arrow {
    animation: bounce 2s infinite;
  }

  @keyframes bounce {
    0%, 20%, 50%, 80%, 100% {
      transform: translateY(0);
    }
    40% {
      transform: translateY(-10px);
    }
    60% {
      transform: translateY(-5px);
    }
  }

  @media (max-width: 768px) {
    .hero {
      min-height: 100vh;
      height: auto;
    }

    .hero-newsletter,
    .hero-scroll {
      position: static;
      margin-top: 2rem;
    }

    .hero-scroll {
      transform: none;
    }
  }
</style>
