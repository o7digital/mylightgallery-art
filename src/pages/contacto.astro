---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout
  title="Contacto – My Light Art Gallery"
  description="Ponte en contacto con My Light Art Gallery"
>
  <section class="contact">
    <div class="container">
      <div class="intro">
        <h1>Contacto</h1>
        <p>
          ¿Tienes preguntas sobre obras, eventos o colaboraciones? Escríbenos y te responderemos a la brevedad.
        </p>
        <div class="details">
          <p><strong>Tel:</strong> +1 (972) 444-4553</p>
          <p><strong>Correo:</strong> info@mylightartgallery.com</p>
          <p><strong>Dirección:</strong> 5215 N. O&apos;Connor Rd. Ste 1725, Irving, TX 75039, USA</p>
        </div>
      </div>

      <form class="form" action="https://formspree.io/f/mgovdlag" method="POST">
        <label>
          Nombre completo
          <input type="text" name="nombre" placeholder="Tu nombre" required />
        </label>
        <label>
          Correo electrónico
          <input type="email" name="email" placeholder="tu@correo.com" required />
        </label>
        <label>
          Teléfono
          <input type="tel" name="telefono" placeholder="+1 (___) ___-____" />
        </label>
        <label>
          Asunto
          <input type="text" name="asunto" placeholder="Sobre qué quieres hablar" />
        </label>
        <label>
          Mensaje
          <textarea name="mensaje" rows="5" placeholder="Cuéntanos más..." required></textarea>
        </label>
        <button type="submit">Enviar</button>
      </form>
    </div>
  </section>
</BaseLayout>

<style>
  .contact {
    padding: 5rem 1.5rem;
    background: #faf8f6;
  }

  .container {
    max-width: 1100px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1.1fr 1fr;
    gap: 3rem;
    align-items: start;
  }

  h1 {
    font-size: 2.3rem;
    margin-bottom: 1rem;
    letter-spacing: 0.04em;
  }

  .intro p {
    color: #444;
    line-height: 1.6;
    margin-bottom: 1rem;
  }

  .details p {
    margin: 0.35rem 0;
    color: #333;
  }

  .form {
    background: #fff;
    padding: 1.75rem;
    border: 1px solid #e8e5e0;
    box-shadow: 0 6px 24px rgba(0, 0, 0, 0.05);
    border-radius: 10px;
    display: grid;
    gap: 1rem;
  }

  label {
    display: grid;
    gap: 0.35rem;
    font-weight: 600;
    color: #222;
    font-size: 0.95rem;
  }

  input,
  textarea {
    width: 100%;
    padding: 0.75rem;
    border: 1px solid #dcd7d0;
    border-radius: 8px;
    font-size: 1rem;
    background: #fff;
    color: #222;
  }

  textarea {
    resize: vertical;
    min-height: 140px;
  }

  input:focus,
  textarea:focus {
    outline: 2px solid #111;
    border-color: #111;
  }

  button {
    padding: 0.9rem 1.2rem;
    background: #111;
    color: #fff;
    border: none;
    border-radius: 8px;
    font-size: 1rem;
    cursor: pointer;
    letter-spacing: 0.02em;
    transition: background 0.25s ease;
  }

  button:hover {
    background: #333;
  }

  @media (max-width: 900px) {
    .container {
      grid-template-columns: 1fr;
    }

    .form {
      padding: 1.4rem;
    }
  }
</style>
